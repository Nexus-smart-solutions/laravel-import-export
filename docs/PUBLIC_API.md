# Current documentation

See [api-reference.md](api-reference.md), [ownership](authorization.md), [notifications](notifications.md), and [verification](VERIFICATION.md). The legacy import API remains supported; the current docs describe the shared metadata engine and restored final layer.
